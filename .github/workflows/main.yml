      - name: Build Kernel (Official Method)
        run: |
          export ARCH=arm64
          export SUBARCH=arm64

          export PATH=$(pwd)/clang/clang-r*/bin:$PATH

          cd kernel

          # Use official build script
          BUILD_CONFIG=common/build.config.gki.aarch64 \
          build/build.sh
